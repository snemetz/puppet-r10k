---

# TODO: rework for current standards
# files::defs:

# Set defaults

# Put in location / env files

# Global defaults
files_default_ensure       : present
#files_default_backup       :
#files_default_force        :
files_default_group        : root
#files_default_ignore       :
files_default_mode         : '0444'
files_default_owner        : root
#files_default_purge        :
#files_default_recurse      :
#files_default_recurselimit :
#files_default_replace      :
#files_default_sourceselect :

files_list_default:
  - root 

files_list_jenkins-images:
  - jenkins-confluence
  - jenkins-jira
  - jenkins-testlink

files_list_jenkins-master:
  - jenkins-images

# Doc all fields

files_defs:
  jenkins-confluence:
    source  : site_workflow/jenkins/images/confluence.png
    path    : /var/lib/jenkins/userContent/confluence.png
    mode    : '0644'
    backup  : false
  jenkins-jira:
    source  : site_workflow/jenkins/images/jira.png
    path    : /var/lib/jenkins/userContent/jira.png
    mode    : '0644'
    backup  : false
  jenkins-testlink:
    source  : site_workflow/jenkins/images/testlink.png
    path    : /var/lib/jenkins/userContent/testlink.png
    mode    : '0644'
    backup  : false
